# Родословное дерево - Веб-приложение

Полнофункциональное веб-приложение для создания и управления родословными деревьями с современным интерфейсом и возможностью экспорта в изображения.

## Возможности

- 🌳 **Интерактивное создание деревьев** - Drag & drop интерфейс для построения родословных
- 👤 **Система авторизации** - Регистрация и вход для сохранения деревьев
- 🎨 **Красивые фоны** - Выбор из различных фоновых изображений (горы, лес, океан, закат)
- 📸 **Экспорт изображений** - Сохранение деревьев в форматах PNG, JPEG, SVG
- 💾 **Сохранение в облаке** - Автоматическое сохранение для зарегистрированных пользователей
- 🚀 **Быстрый старт** - Создание деревьев без регистрации
- 📱 **Адаптивный дизайн** - Работает на всех устройствах

## Технологический стек

### Фронтенд
- **React 19** - Современная библиотека для UI
- **Tailwind CSS** - Utility-first CSS фреймворк
- **React Flow** - Библиотека для интерактивных диаграмм
- **Shadcn/UI** - Компоненты пользовательского интерфейса
- **React Query** - Управление состоянием сервера
- **React Router** - Маршрутизация

### Бэкенд
- **Flask** - Легковесный веб-фреймворк Python
- **SQLite** - Встроенная база данных
- **JWT** - Аутентификация и авторизация
- **Flask-CORS** - Поддержка CORS

### Инфраструктура
- **Docker** - Контейнеризация приложения
- **Nginx** - Веб-сервер для фронтенда
- **Docker Compose** - Оркестрация сервисов

## Быстрый старт

### Предварительные требования

- Docker и Docker Compose
- Git

### Установка и запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd family-tree-app
   ```

2. **Запустите приложение:**
   ```bash
   docker-compose up -d
   ```

3. **Откройте в браузере:**
   ```
   http://localhost
   ```

Приложение будет доступно по адресу `http://localhost`, а API по адресу `http://localhost:5001`.

### Разработка

#### Запуск в режиме разработки

**Бэкенд:**
```bash
cd backend/family-tree-api
pip install -r requirements.txt
python src/main.py
```

**Фронтенд:**
```bash
cd frontend/family-tree-frontend
pnpm install
pnpm run dev
```

#### Структура проекта

```
family-tree-app/
├── backend/                 # Flask API сервер
│   ├── family-tree-api/    # Исходный код API
│   │   ├── src/           # Python модули
│   │   └── requirements.txt
│   └── Dockerfile         # Docker образ для бэкенда
├── frontend/              # React приложение
│   ├── family-tree-frontend/ # Исходный код фронтенда
│   │   ├── src/          # React компоненты
│   │   └── public/       # Статические файлы
│   ├── Dockerfile        # Docker образ для фронтенда
│   └── nginx.conf        # Конфигурация Nginx
├── docker-compose.yml    # Оркестрация сервисов
└── README.md            # Документация
```

## API Документация

### Аутентификация

- `POST /api/auth/register` - Регистрация пользователя
- `POST /api/auth/login` - Вход в систему
- `GET /api/auth/me` - Получение информации о пользователе

### Деревья

- `GET /api/trees` - Получение деревьев пользователя
- `POST /api/trees` - Создание нового дерева
- `GET /api/trees/{id}` - Получение дерева по ID
- `PUT /api/trees/{id}` - Обновление дерева
- `DELETE /api/trees/{id}` - Удаление дерева
- `POST /api/trees/anonymous` - Создание анонимного дерева

## Использование

### Создание дерева

1. Откройте приложение в браузере
2. Нажмите "Создать дерево бесплатно" для быстрого старта
3. Или зарегистрируйтесь для сохранения деревьев

### Добавление людей

1. Нажмите "Добавить человека"
2. Заполните информацию (имя, пол, годы жизни)
3. Перетаскивайте узлы для изменения позиции
4. Соединяйте узлы для создания связей

### Настройка внешнего вида

1. Нажмите "Настройки"
2. Выберите фоновое изображение
3. Настройте другие параметры отображения

### Экспорт изображения

1. Нажмите "Скачать"
2. Выберите формат (PNG, JPEG, SVG)
3. При необходимости измените фон
4. Нажмите "Скачать изображение"

## Конфигурация

### Переменные окружения

**Бэкенд:**
- `FLASK_ENV` - Режим работы (development/production)
- `DATABASE_URL` - URL базы данных
- `JWT_SECRET_KEY` - Секретный ключ для JWT

**Фронтенд:**
- `VITE_API_URL` - URL API сервера

### Настройка продакшена

1. Измените `JWT_SECRET_KEY` в docker-compose.yml
2. Настройте SSL сертификаты для HTTPS
3. Настройте резервное копирование базы данных
4. Настройте мониторинг и логирование

## Лицензия

MIT License

## Поддержка

Если у вас возникли вопросы или проблемы, создайте issue в репозитории проекта.

